#!/bin/sh
# Replays a recorded claude session instead of running the real CLI (see ../mock-agent.mjs).
# Put this folder first on PATH, or point AGENTHYDRA_CLAUDE_PATH at this file.
exec bun "$(dirname "$0")/../mock-agent.mjs" --as claude "$@"
